# TITAN LCE — Environment Variables
# Copy to .env and fill in real values

# PostgreSQL
POSTGRES_PASSWORD=change_me_strong_password

# Elixir Bridge
SECRET_KEY_BASE=generate_with_mix_phx_gen_secret_64_chars
CLOAK_KEY=generate_with_32_random_bytes_base64_encoded
LCE_CORE_TOKEN=shared_token_between_core_and_bridge
LCE_WEBHOOK_SECRET=erp_webhook_hmac_secret
LCE_API_TOKEN=bearer_token_for_api_access

# Optional
# LCE_SIMULATE_DEVICES=true
# LCE_HOST_ALIAS=host.docker.internal
